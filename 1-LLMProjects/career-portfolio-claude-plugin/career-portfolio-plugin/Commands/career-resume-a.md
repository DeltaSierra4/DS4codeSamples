---
description: Build Resume A — the non-technical résumé for events, guest-experience, community, and fashion roles (.docx + .pdf).
argument-hint: [optional: target role, e.g. "Director of Guest Experience"]
---

Invoke the `resume-a` skill and begin its workflow now. First resolve the source folder with the three-option dialogue (current directory / a different path / something else). If the user gave a target role in "$ARGUMENTS", use it as the role to tailor for. Remember: sample copy in `reference.md` is an illustrative draft to reword in the user's voice, not verbatim output.